⌿definitions⌿Activity ->
	TypeDef(⌿definitions⌿Activity, Seq(
		Field(⌿definitions⌿Activity⌿actions, Opt(Str))))
⌿definitions⌿Example ->
	TypeDef(⌿definitions⌿Example, Seq(
		Field(⌿definitions⌿Example⌿messages, Opt(Arr(Arr(TypeDef(⌿definitions⌿Activity, Seq(
			Field(⌿definitions⌿Activity⌿actions, Opt(Str)))))))), 
		Field(⌿definitions⌿Example⌿nestedArrays, Opt(Arr(Arr(Arr(Arr(Str))))))))